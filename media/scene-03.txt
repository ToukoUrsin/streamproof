Now we use a clearly labeled programmed control. One patch has been deliberately recolored, while the bank stays fixed. OpenCV aligns features outside the water, checks the background for a lighting shift, and measures the overlapping region. About sixty-seven percent of its pixels exceed the display-distance threshold. The policy asks for a close view, an upstream view and context. The overlay and decision trace show exactly why. No pollution diagnosis is generated.
